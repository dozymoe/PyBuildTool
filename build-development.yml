pybuildtool:
    options:
        pylint_config_file: etc/pylint.rc

    addons:
        pylint:
            raw_file_in: "{_1}/{_2}/*.py"

    core:
        pylint:
            raw_file_in: "{_1}/{_2}/*.py"

    misc:
        pylint:
            raw_file_in: "{_1}/{_2}/*.py"

    tools:
        pylint:
            raw_file_in: "{_1}/{_2}/*.py"
